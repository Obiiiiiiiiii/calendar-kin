You read a chat transcript between a user and a fictional AI companion (the "kin") and detect PLANNABLE FUTURE EVENTS in the kin's own life that the kin mentioned in plain dialogue — e.g. "I've got that gig Thursday" or "I'm covering the morning shift tomorrow." These will be written to a calendar the kin reads as ITS OWN.

RULES:
1. Only events the KIN states or clearly commits to about ITS OWN life. Never the user's plans, never hypotheticals ("maybe I should..."), never invitations the kin merely floats without commitment.
2. Only FUTURE events with a determinable date within the next 7 days from today. Resolve relative words ("tomorrow", "Thursday") using the date table below. If the date cannot be pinned down, do not emit a candidate.
3. If the kin gives no time, choose a sensible in-character time and mark confidence "medium" or lower. If both date and time are explicit, confidence may be "high".
4. Self-attribution: titles and descriptions are written in third person by the kin's name (e.g. "Derek plays his gig at the Blue Room"), one or two tight in-world sentences for the description, location when stated.
5. Quote the exact line of dialogue each candidate came from in "quote".
6. Most candidates are type "oneoff" with a "date". Only use type "standing" with "days_of_week" if the kin explicitly describes a new recurring commitment.
7. When in doubt, emit NOTHING. A missed event is recoverable; a wrong event becomes false canon the kin treats as true. An empty candidates list is a perfectly good answer.

Today is {{TODAY}} ({{TODAY_WEEKDAY}}), timezone {{TIMEZONE}}.
Date table for resolving weekday names:
{{DATE_TABLE}}

Output ONLY valid JSON matching this schema. No preamble, no fences.

Schema:
{
  "candidates": [
    {
      "title": "string (self-attributing)",
      "type": "oneoff|standing",
      "date": "YYYY-MM-DD (for oneoff; null for standing)",
      "days_of_week": ["Mon","Tue","Wed","Thu","Fri","Sat","Sun"],
      "start": "HH:MM",
      "end": "HH:MM",
      "location": "string or null",
      "gates_availability": true,
      "description": "string (in-world, self-attributing, 1-2 sentences)",
      "quote": "string (the dialogue line this came from)",
      "confidence": "high|medium|low"
    }
  ]
}

Transcript (oldest first):
{{TRANSCRIPT}}
